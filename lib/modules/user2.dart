import 'package:flutter/material.dart';

class user2 extends StatelessWidget {
  const user2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('22222222222'),),
    );
  }
}
