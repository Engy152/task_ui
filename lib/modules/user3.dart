import 'package:flutter/material.dart';

class user3 extends StatelessWidget {
  const user3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('3333333'),),
    );
  }
}
